type Query {
  nop: Boolean
  currentUser: WebappUser
  getUsers(filter: WebappUserFilterInput): [WebappUser]
}

type Subscription {
  # Base subscription type - extended by plugins
  _empty: String
}
